// A implementação real vive em packages/ds (pacote @brunosantossss/ds).
// Este arquivo existe só para não quebrar imports antigos ("@/components/ui/Tooltip").
export { Tooltip } from "@brunosantossss/ds";
export type { TooltipProps,TooltipPosition } from "@brunosantossss/ds";
